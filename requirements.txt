# Core science + viz (pinned) — in requirements-core.txt
-r requirements-core.txt

# Marimo notebook framework
marimo>=0.10.0

# Marimo optional features
vegafusion[embed]          # server-side Vega charts
vl-convert-python          # Vega-Lite chart export
openai                     # AI assistant features
mcp                        # MCP server connectivity
python-lsp-server>=1.13.0   # Language Server Protocol
python-lsp-ruff>=2.0.0      # Ruff linting plugin for pylsp
pylsp-mypy                   # MyPy plugin for pylsp
websockets                   # LSP transport

# Dev tools
ruff>=0.9.0
